{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and test",
      "type": "shell",
      "command": "clear && (make --quiet --jobs || (clear && make))",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
